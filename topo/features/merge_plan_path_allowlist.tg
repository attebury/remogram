feature feature_merge_plan_path_allowlist {
  name "Merge plan path allowlist"
  description "Evaluate PR changed-file paths against glob allowlists during merge planning."
  intent "Autonomous Observer and Merge Lane can fail closed on path scope violations via remogram merge plan blockers."
  entities []
  capabilities [cap_forge_remote_ops]
  endpoints []
  seed_data []
  verification_refs [verification_merge_plan_path_allowlist]
  status draft
}
