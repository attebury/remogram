plan plan_test_strategy_round3 {
  name "Test Strategy Round 3 Plan"
  description "Test hardening from 2026-06-10 code review; implement on impl/test-strategy-round3 after git helpers merge."
  task task_test_strategy_round3
  architecture_patterns [contract_boundary, command_owned_mutation]
  priority medium
  steps {
    step mcp_security_battery status pending description "ref_compare invalid ref, pr_checks, truncation tests in tools.test.mjs."
    step fetch_json_redirect status pending description "Direct fetchJson redirect test in http.test.mjs."
    step env_isolation status pending description "GITLAB_TOKEN cleanup in default-providers afterEach."
    step coverage_policy_docs status pending description "README and coverage-config test document core-only scope."
    step real_provider_cli status pending description "Optional commands-real-provider.test.mjs with temp forge."
    step verify status pending description "npm test with env tokens unset."
  }
  status draft
}
