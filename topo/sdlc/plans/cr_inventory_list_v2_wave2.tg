plan plan_cr_inventory_list_v2_wave2 {
  name "CR inventory list v2 — wave 2 GitLab"
  description "GitLab X-Total header fast path and sort mapper with provider tests."
  task task_cr_inventory_list_v2_wave2
  architecture_patterns [contract_boundary, fail_closed_gate]
  steps {
    step gitlab_fast_path status done description "X-Total fast path and order_by/sort mapping."
    step verify status done description "gitlab-api tests and npm test green."
  }
  status complete
}
