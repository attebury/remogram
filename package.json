{
  "name": "remogram",
  "version": "0.1.0-beta.10",
  "private": true,
  "description": "Generic SCM/forge boundary CLI and MCP — provider-attributed forge facts only",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/attebury/remogram.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:guard": "node ./scripts/check-test-append-only.mjs --skip-diff",
    "test:guard:full": "node ./scripts/check-test-append-only.mjs",
    "test:coverage": "vitest run --coverage",
    "security:secrets": "node ./scripts/run-secret-scan.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
